#Like if simple want to import one or 2 ojects from an import, then we can simply use it like this.
from math import factorial

print(factorial(6))##Therefore we can't use other objects(i.e functions) of math like gcd, log,etc as they are not imported
